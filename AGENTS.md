# degrade-guard-agent

コード変更に対して「変えていないはずの既存処理」が壊れていないか（＝デグレ）を、
**机上（静的推論）だけで** 検証してレビュー観点として提示するエージェント／スキル。

## これは何か

- 変更差分そのものの良し悪しではなく、**その"外側"の既存挙動が不変か** を論証する。
- **テストは実行しない。** 机上推論のみ。テストが無いプロジェクトでも使える。
- AI は判定を下さず、**人間が確認すべき点を漏れなく並べる**。

## 使い方

デグレを確認したい変更（diff / 変更前後のコード）を渡して、机上デグレ検証を依頼する。
手順・出力フォーマットの詳細はスキルに定義してある。

- スキル：`degrade-guard`（`.claude/skills/degrade-guard/SKILL.md`）

## 原則

1. 変更差分を「入力空間の分割（影響を受ける／受けない）」として捉え、補集合の不変を論証する。
2. 変更の意図はまず人間に尋ねる（引き出せない場合のみ AI が推測し、推測と明示する）。
3. 波及は呼び出し元1ホップ。ただし「型が同じ＝安全」ではなく、振る舞い（出力）を見る。
4. 机上トレース表で検証を可視化し、判定を ✅不変 / ⚠️要確認 / 🔴変化 の3段に割る。
5. 言い切れないものは断定せず ⚠️ として人間へエスカレーションする。

## 両対応（Claude Code / Codex CLI）

- スキル本体の正本は `.claude/skills/degrade-guard/SKILL.md`。
- Codex CLI は `.codex/skills/degrade-guard`（→ 正本への symlink）から同じ実体を読む。
- 詳細は README.md のインストール手順を参照。
